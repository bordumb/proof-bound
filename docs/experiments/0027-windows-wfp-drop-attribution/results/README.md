# Experiment 0027 checked-in results

These compact payloads are canonical copies from
[GitHub run 33844439146](https://github.com/bordumb/proof-bound/actions/runs/33844439146):

- `execution.json` records the final `revise` decision, question values,
  metrics, identities, and validator agreement;
- `report.json` is the byte-identical semantic report emitted independently by
  Python and Rust; and
- `attacks.json` is their byte-identical classification of all 48 mutations.

The 457,009-byte raw capture and per-attack captures remain in the retained
workflow artifact. See the [artifact ledger](../ARTIFACTS.md) for identities.
