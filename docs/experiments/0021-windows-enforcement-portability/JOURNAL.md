# Experiment 0021 journal

- **2026-09-03 — Registration.** Registered the Windows policy and live-boundary
  questions before implementation. Non-Windows execution must remain an
  unanswered result rather than a simulated pass.
- **2026-09-03 — Candidate compilation.** Independently compiled the common
  contract to a conjunctive AppContainer, restricted-token, job-object, and
  exact-ACL policy. Neither compiler treats the policy model as execution.
- **2026-09-03 — Availability gate.** The registered host is Darwin arm64, not
  Windows 11 arm64/x86_64. The executor stopped before tool or workload
  execution, retained zero receipts, and recorded that no fallback ran.
- **2026-09-03 — Independent validation.** Rust and Python emitted identical
  reports and rejected all 18 registered attacks exactly. Decision:
  `unanswered`.
