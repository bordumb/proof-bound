{
  "schema": "proofbound-research-rust-sampling-preregistration/1",
  "experiment": "EXP-0007",
  "revision": 1,
  "status": "preregistered-not-executed",
  "date": "2026-09-02",
  "subject": "git:6a355180aa47bda23408f5de2a20fd10f6234448",
  "framework": {"name": "proptest", "version": "1.11.0"},
  "frozen_sources": [
    {"path": "demo/allowance/rust/kernel/src/lib.rs", "sha256": "sha256:2b1f51ec656f207dafc4de40b833de417bcd9c5c3f84a571f93334b745f64490", "size_bytes": 7172},
    {"path": "demo/allowance/proofbound/evidence/rust-kernel-tests.toml", "sha256": "sha256:70b99da238ff866bc6025469b6ddab34e2216ce64029b553b1bd442b8d223223", "size_bytes": 1090}
  ],
  "registered_execution": {
    "seed": {"encoding": "decimal-u64", "value": 424242},
    "successful_cases": 100,
    "rng_algorithm": "chacha",
    "persistence": "disabled",
    "shrinking": "enabled",
    "max_shrink_iterations": 10000,
    "max_local_rejects": 1000,
    "max_global_rejects": 1000
  },
  "candidate_contract": "proofbound-sampling-contract/1",
  "candidate_observation": "proofbound-sampling-observation/1",
  "attacks": [
    {"id": "EXP-0007-A001", "mutation": "replace the fixed seed", "expected": "sampling-contract-mismatch"},
    {"id": "EXP-0007-A002", "mutation": "replace the RNG algorithm while retaining seed and version", "expected": "holdout-contract-insufficient-or-mismatch"},
    {"id": "EXP-0007-A003", "mutation": "enable file failure persistence", "expected": "sampling-contract-mismatch"},
    {"id": "EXP-0007-A004", "mutation": "replace the successful-case budget", "expected": "sampling-contract-mismatch"},
    {"id": "EXP-0007-A005", "mutation": "substitute the generator entrypoint and recompute the outer contract identity", "expected": "generator-identity-mismatch"},
    {"id": "EXP-0007-A006", "mutation": "change one generator-closure byte", "expected": "generator-identity-mismatch"},
    {"id": "EXP-0007-A007", "mutation": "substitute one same-count target", "expected": "sampling-inventory-mismatch"},
    {"id": "EXP-0007-A008", "mutation": "count a shrink replay as a fresh attempted case", "expected": "sampling-report-invalid"},
    {"id": "EXP-0007-A009", "mutation": "replace the minimal counterexample while retaining the original failure identity", "expected": "sampling-report-invalid"},
    {"id": "EXP-0007-A010", "mutation": "relabel a counterexample as passed", "expected": "sampling-report-invalid"},
    {"id": "EXP-0007-A011", "mutation": "replace framework version and keep the framework name", "expected": "sampling-tool-mismatch"},
    {"id": "EXP-0007-A012", "mutation": "reinterpret the existing rust-kernel-tests receipt as explicit sampling", "expected": "sampling-schema-mismatch"}
  ],
  "decision": {
    "confirm": "Q1, Q2, and Q3 pass with the unchanged common contract and both independent validators reject all attacks.",
    "extend": "A new field is required, has backend-neutral semantics, and can be populated authoritatively by Hypothesis, fast-check, and proptest.",
    "reject": "Required meaning is available only through private/prose APIs or a Rust-specific common-kernel branch."
  }
}
