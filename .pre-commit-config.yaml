minimum_pre_commit_version: "3.6.0"

repos:
  - repo: local
    hooks:
      - id: proofbound-fast-checks
        name: Proofbound fast checks
        entry: bash tools/ci/pre-commit.sh
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
