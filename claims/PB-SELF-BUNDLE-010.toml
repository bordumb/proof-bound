schema = "proofbound-claim/1"
id = "PB-SELF-BUNDLE-010"
title = "Tool bundles bind one reproduced payload to verified source"
statement = "A Proofbound tool-bundle candidate identifies one exact source revision and successful mainline verification run, contains the closed required payload, and is rejected when its reproduced binaries, archive inventory, or member identities differ."
subject = "python:tools.release.build_tool_bundle"
profile = "ledger"
tier = 0
evidence = ["test:tool-bundle-production"]
assumptions = []
open_obligations = ["Hosted runner, compiler, linker, archive implementation, and SHA-256 behavior remain trusted and explicitly identified rather than proved."]
out_of_scope = ["Publisher authentication and behavior of the bundled executables."]
source_roots = [
  ".github/workflows/tool-bundle.yml",
  "schemas/tool-bundle-manifest.schema.json",
  "schemas/tool-bundle-publication-manifest.schema.json",
  "tools/release/build_tool_bundle.py",
  "tools/release/validate_verification_run.py",
]
