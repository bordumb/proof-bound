/-!
This handwritten module intentionally contains no refinement theorem yet.

After `translation-unit.toml` produces the generator-owned declarations,
import those declarations here, define the richer project relation, and prove
`YourProject.Kernel.withdraw_refines`. Only then attach
`@[proofbound_claim "EXAMPLE-KERNEL-001"]` and register this file as theorem
evidence. A theorem over a handwritten stand-in must not be presented as a
source-refinement result.
-/
